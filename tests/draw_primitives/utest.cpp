#include "mu_test.h"

#include <string>
#include <map>
#include <vector>
#include <iostream>
#include <cstddef>
#include <climits>

#include "draw_primitives.hpp"


// BEGIN_TEST(drawCircle)
// 	// advcpp::ColorImage newImage1 = advcpp::LoadImageFromFile("cat.ppm");
// 	// advcpp::DrawCircle(newImage1, advcpp::Color{0, 0, 0}, advcpp::Point{100, 150}, 50);
//     // advcpp::SaveImageToFile("circle.ppm", newImage1);

// 	// advcpp::ColorImage newImage2 = advcpp::LoadImageFromFile("cat.ppm");
// 	// advcpp::DrawLine(newImage2, advcpp::Color{0, 0, 0}, advcpp::Point{100, 150}, advcpp::Point{100, 300});
//     // advcpp::SaveImageToFile("vertical.ppm", newImage2);

// 	// advcpp::ColorImage newImage2 = advcpp::LoadImageFromFile("cat.ppm");
// 	// advcpp::DrawLine(newImage2, advcpp::Color{0, 0, 0}, advcpp::Point{100, 150}, advcpp::Point{200, 150});
//     // advcpp::SaveImageToFile("horizontal.ppm", newImage2);

// 	// advcpp::ColorImage newImage2 = advcpp::LoadImageFromFile("cat.ppm");
// 	// advcpp::DrawLine(newImage2, advcpp::Color{0, 0, 0}, advcpp::Point{100, 150}, advcpp::Point{200, 250});
//     // advcpp::SaveImageToFile("line.ppm", newImage2);

// END_TEST




TEST_SUITE(因果応報 [inga ōhō: bad causes bring bad results])
	//TEST(drawCircle)


END_SUITE
